# Required due to peer dependency conflicts:
# - Yoopta Editor expects slate@^0.102.0 but we use slate@^0.110.2 (newer version with bug fixes)
# - Radix UI (via Excalidraw) expects React 16-18 but we use React 19.2
# Both libraries work correctly with these newer versions despite the warnings.
legacy-peer-deps=true
